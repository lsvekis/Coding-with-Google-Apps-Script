function createHeadings() {
 const doc = DocumentApp.openById(DOCID);
 const body = doc.getBody();
 body.appendParagraph('Main Heading').setHeading(DocumentApp.ParagraphHeading.HEADING1);
 body.appendParagraph('Subheading').setHeading(DocumentApp.ParagraphHeading.HEADING2);
}
